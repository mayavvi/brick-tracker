# Path to the Projects02 root directory
# Windows (local dev):
PROJECTS_BASE_PATH=E:\Dev_2026\mnt\Development\Projects02
# Linux / Posit Connect (production):
# PROJECTS_BASE_PATH=/mnt/Development/Projects02

# Cache TTL in seconds (default: 300)
CACHE_TTL_SECONDS=300

# SQLite database path (default: data/tracker.db relative to project root)
# DATABASE_PATH=data/tracker.db

# Local development: default username (bypasses Posit Connect auth)
DEV_USERNAME=dev-user
